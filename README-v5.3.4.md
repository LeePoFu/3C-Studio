# v5.3.4

- All duel setup controls use the same explicit height variable.
- Rebalanced score, current-run status, +1/-1 and quick-score control heights.
- Rebuilt equal-inning result resolution and made summary display fail-safe before locking the game state.
